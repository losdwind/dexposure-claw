mcp_servers:
  dexposure:
    command: "{{ mcp_command }}"
    args:
      - "mcp"

# Optional: copy DeXposure Claw skills into ~/.hermes/skills/dexposure/
# or configure Hermes external skill directories to point at pack/skills.
