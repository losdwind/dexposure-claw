# cloud/train_src/

Verbatim runtime copy of the FM training entrypoint for cloud runs.
Source of truth: `archive/code/run_full_experiment.py` (+ `dexposure_fm/`),
which is byte-identical to branch `paper/icml2026-camera-ready`.
Copied here because `.skyignore` excludes `archive/` from workdir sync.
Do not edit -- re-copy from archive/code if the snapshot ever changes.
