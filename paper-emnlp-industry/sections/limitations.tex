\section*{Limitations}
\label{sec:limitations}

\paragraph{Single domain.}
\benchname{} evaluates a single on-chain risk surface (DeFi
inter-protocol credit exposure). Generalisation to NFT lending,
perpetual derivatives, or cross-chain bridge networks is untested
and will require domain-specific re-calibration of the conformal
split and the stressed-set percentile $\pi$.

\paragraph{Weekly resolution.}
The pipeline operates at weekly granularity, but several DeFi shocks
unfold within hours; Terra/Luna erased \$40B in under 48~hours. A
sub-hourly production deployment would require denser snapshot
ingestion and re-evaluation of conformal calibration on the
resulting denser time series. The $4$--$5$ week lead times we
report should be read as ``how early the weekly monitor turns'',
not as end-to-end alert latency.

\paragraph{Judge configuration and calibration.}
Our judge panel runs at \emph{minimal} reasoning effort for a
like-for-like comparison against the non-reasoning baseline
(Appendix~\ref{app:implementation}); default reasoning settings would
shift the reported scores, so practitioners should re-calibrate the
thresholds accordingly. More fundamentally, all judge scores are from
LLM panels: human-judge calibration with practising supervisors
remains the most pressing methodological extension.

\paragraph{Decision-outcome proxy.}
\texttt{b5\_decision} scores tickets against a regulator-aligned
absolute-loss ground truth, which captures supervisory targeting
priority but not the counterfactual effect of supervisory
action, i.e., whether the recommended risk reduction would have
changed the subsequent contagion path. We therefore interpret decision
scores as evidence of targeting and explanation quality, not as a
causal estimate of intervention impact.
