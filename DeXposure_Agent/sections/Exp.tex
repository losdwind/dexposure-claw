\section{Experiments}
\label{sec:exp}

We evaluate \textsc{DeXposure-Framework} on the \textsc{DeXposure-Bench} suite (\S\ref{sec:bench}), grouping the six benchmarks under two natural axes: (i)~does the forecast--measure pipeline produce accurate, well-calibrated, and timely risk signals (Task~I, covering b1, b2, b3, b6), and (ii)~do the resulting decision tickets improve upon baselines in precision, stability, and auditability (Task~II, covering b4, b5)? The ground truth, dataset split, and reference methods used throughout this section are defined in \S\ref{sec:bench}; all results are reported on the frozen test split of Table~\ref{tab:bench-split}.

\subsection{Task I: Network Risk Monitoring}

\subsubsection{Implementation Details}
\label{sssec:impl_task1}

\paragraph{Data and temporal granularity.}
We use the \textsc{DeXposure} dataset~\cite{wu2025dexposuredatasetbenchmarksinterprotocol}, comprising weekly directed weighted exposure-graph snapshots sourced from the DeFiLlama API. The dataset spans 2020-03 to 2025-08 (283~weeks), with 50--500 protocol nodes and 100--2{,}000 exposure edges per snapshot, totalling over 43.7M data entries. Node features include log-TVL, number of token types, maximum token share, Shannon entropy of token distribution, and protocol category (one-hot, 15~classes). Edge weights are log-scaled USD exposure values. We evaluate multi-horizon forecasting on $\mathcal{H}=\{1,4,8,12\}$ weeks.

\paragraph{Temporal split.}
We enforce a strict no-leakage temporal split:
\begin{center}
\footnotesize
\setlength{\tabcolsep}{3pt}
\begin{tabularx}{\linewidth}{@{}llrX@{}}
\toprule
Split & Period & Weeks & Usage \\
\midrule
Train & 2020-03 -- 2024-06 & $\sim$222 & FM backbone training \\
Val   & 2024-07 -- 2024-12 & $\sim$26  & Hyperparameter + threshold tuning \\
Test  & 2025-01 -- 2025-08 & $\sim$33  & Final evaluation \\
\bottomrule
\end{tabularx}
\end{center}
%We additionally report expanding-window walk-forward results (15~folds, 4-week evaluation windows starting 2022-07) for robustness.

\paragraph{Stress event windows.}
For early-warning evaluation (b2_warning), we define ground-truth stress periods around three major DeFi crises:
\begin{center}
\footnotesize
\setlength{\tabcolsep}{3pt}
\begin{tabularx}{\linewidth}{@{}lccc@{}}
\toprule
Event & Date & Pre-shock (4w) & Event (3w) \\
\midrule
Terra/Luna & 2022-05-09 & 03-28 -- 05-02 & 05-02 -- 05-23 \\
FTX        & 2022-11-07 & 09-26 -- 10-31 & 10-31 -- 11-21 \\
SVB/USDC   & 2023-03-10 & 02-06 -- 03-06 & 03-06 -- 03-27 \\
\bottomrule
\end{tabularx}
\end{center}

\paragraph{Forecast interface and predicted-graph construction.}
For each epoch $t$ and horizon $h\in\mathcal{H}$, we query the backbone model to obtain a predictive distribution $\mathcal{P}_{t,h}$. We construct an expected-weight predicted graph $\widehat{G}_{t+h}$ by thresholding edge existence probabilities at $\pi_{\min}$ and setting $\widehat{w}_{pq} := \Pr(e_{pq}=1)\cdot \mathbb{E}[w_{pq}\mid e_{pq}=1]$ (Eq.~\eqref{eq:expected_graph}). We set $\pi_{\min}=0.2$ on the validation split. For uncertainty quantification, we draw $M=50$ Monte Carlo samples via Bernoulli edge sampling and Gaussian weight perturbation.

\paragraph{Monitoring metrics and alert triggers.}
At each horizon, we compute five monitoring functionals $\mathbf{M}_{t,h}=\Phi(\widehat{G}_{t+h},X_t)$ (Table~\ref{tab:metrics}): systemic importance (M1), HHI concentration (M3), network density (M4), PageRank Gini (M6), and degree Gini (M7). Baseline bands $(\mu_t^{(j)}, \sigma_t^{(j)})$ are computed via a rolling window of $L=26$ weeks. An alert fires when a metric exceeds $z=2$ standard deviations from baseline (Eq.~\eqref{eq:alert_trigger}). Alert confidence follows Eq.~\eqref{eq:alert_confidence}.

\paragraph{Stress scenarios.}
We evaluate five standardised scenarios $\mathcal{S}_0$ (Table~\ref{tab:scenarios}): single protocol failure (S1, 100\% shock on top node), bridge cluster failure (S2, 100\% on bridges), stablecoin de-peg (S3, 50\% on stablecoins), sector-wide lending shock (S4, 30\%), and correlated stress (S5, 20\% on top-10 nodes). CVaR is computed as the mean of the worst $\lambda=0.5$ fraction of MC sample losses (Eq.~\eqref{eq:cvar}).

\paragraph{Attribution.}
For each triggered alert, we rank contributing edges by marginal contribution: $\mathrm{Contrib}(e) := \Phi^{(j)}(\widehat{G}_{t+h}) - \Phi^{(j)}(\widehat{G}_{t+h}\setminus e)$, retaining the top $K=10$.

\paragraph{Evaluation protocol.}
Figure~\ref{fig:eval_framework} summarises the evaluation framework. We evaluate Task~I along three axes:
\begin{enumerate}[leftmargin=*,itemsep=1pt]
    \item \emph{Risk-metric forecasting (b1_forecast):} predicted vs.\ realised metrics using MAE and Spearman rank correlation, for each $h\in\{1,4,8,12\}$.
    \item \emph{Early-warning quality (b2_warning):} lead time to stress events, precision/recall under alert budgets $\{5,10,20\}$, alert stability (flip rate), and F1-warning.
    \item \emph{Uncertainty calibration (b3_calibration):} expected calibration error (ECE), prediction interval coverage (target 90\%), PI width, and CRPS. We apply split conformal prediction on the validation set to ensure asymptotically valid coverage.
\end{enumerate}

\begin{figure*}[!t]
    \centering
    \includegraphics[width=0.82\textwidth]{figures/fig3_evaluation_framework}
    \caption{Evaluation framework: six benchmarks (b1_forecast--b6_robustness) covering risk forecasting, early warning, uncertainty calibration, stress testing, decision quality, and robustness.}
    \label{fig:eval_framework}
\end{figure*}

\paragraph{Reproducibility.}
All hyperparameters ($\pi_{\min}$, $L$, $z$, $K$, $M$, $\tau_{\mathrm{data}}$) are selected on the validation split and fixed for test evaluation. Random seeds are fixed for MC sampling; we report mean $\pm$ standard deviation over $R=3$ independent runs.
All benchmark results in this section are pending regeneration after the experiment-audit cleanup. The cleaned pipeline uses fail-closed predictor routing: unavailable FM or EvolveGCN checkpoints now abort the corresponding run rather than falling back to persistence. b2_warning is reported as a shared weighted-degree heuristic (h1_weighted_degree) rather than as a method-specific comparison. Final runtime and hardware details will be filled in after the clean rerun.

\subsubsection{Empirical Results}
Table~\ref{tab:task1_summary} and Table~\ref{tab:b1_all_horizons} are placeholders until the clean rerun completes. The regenerated Task~I comparison will report m5_fm_rules (FM+Rules), m1_persistence_rules (Persistence+Rules), h1_weighted_degree for b2_warning, and m3_evolvegcn only if trained EvolveGCN checkpoints are present.

\input{sections/Table_TaskI_Summary.tex}

\input{sections/Table_b1_forecast_AllHorizons.tex}

\begin{figure}[!t]
    \centering
    \includegraphics[width=0.85\linewidth]{figures/fig5_task1_b1_rankcorr}
    \caption{Task~I b1_forecast rank correlation across forecast horizons.}
    \label{fig:task1_b1_rankcorr}
\end{figure}

The previous numerical Task~I discussion has been removed because it was tied to stale artifacts, including a m3_evolvegcn run that matched persistence after metadata removal. We will restore this paragraph only after the new result manifest passes the audit script.

\subsection{Task II: Decision-Making}

\subsubsection{Implementation Details}
\label{sssec:impl_task2}

\paragraph{Decision timeline and inputs.}
Task~II operates at the same decision epochs as Task~I. At each epoch, the decision agent receives: (i)~the current state $(G_t,X_t)$, (ii)~multi-horizon forecasts $\{\widehat{G}_{t+h}\}_{h\in\mathcal{H}}$ and optional MC samples, (iii)~monitoring alerts $\mathcal{A}_t$ with evidence and confidence scores, and (iv)~the scenario summary $\mathcal{R}_t$.

\paragraph{Action space and playbook.}
We use a four-level conservative playbook (Table~\ref{tab:playbook}): \texttt{Monitor} (severity weight 0.25, always feasible), \texttt{Investigate} (0.50, always feasible), \texttt{Recommend-Reduce} (0.75, requires $\mathrm{SAFE\_MODE}=0$ and $\ge 2$ alerts), and \texttt{Contingency} (1.00, requires $\mathrm{SAFE\_MODE}=0$ and $\ge 3$ alerts). Intervention-type actions additionally require mean alert confidence $\bar{C}_t \ge \tau_{\mathrm{conf}}=0.6$.

\paragraph{Ticket scoring.}
Tickets are ranked by $\mathrm{Score}_t(u) = w_u \cdot \bar{C}_t \cdot \mathrm{Imp}_t$ (Eq.~\eqref{eq:ticket_score}), where $w_u$ is the playbook severity weight, $\bar{C}_t$ is mean alert confidence, and $\mathrm{Imp}_t$ is the mean CVaR across all scenarios (defaulting to~1.0 when no scenarios are available). We output the top-$N=10$ tickets.

\paragraph{Target selection.}
For intervention tickets, targets are selected from the union of top-$K$ attributed nodes across triggering alerts and worst-case scenarios, ranked by frequency-weighted vote.

\paragraph{Evaluation protocol.}
We evaluate Task~II using both offline and scenario-based criteria:
\begin{enumerate}[leftmargin=*,itemsep=1pt]
    \item \emph{Offline (b5_decision):} ticket precision (fraction followed by real stress), target stability (Jaccard between adjacent epochs), audit completeness (evidence bundle coverage), suppression rate (safe-mode activation), and false intervention rate.
    \item \emph{Scenario-based (b4_stress):} stress-test contagion prediction accuracy---loss MAE, distressed count MAE, propagation depth MAE, and target overlap@$K$ between predicted and actual networks under scenarios S1--S5.
\end{enumerate}

\paragraph{Reproducibility.}
All decision hyperparameters ($K$, $N$, $\tau_{\mathrm{data}}$, $\tau_{\mathrm{conf}}$, $\lambda$) are tuned on the validation split and fixed for testing. MC components use fixed seeds; we report mean $\pm$ standard deviation over $R=3$ runs.

\subsubsection{Empirical Results}
Table~\ref{tab:task2_table3} is pending clean regeneration. The rerun will compare m1_persistence_rules (Persistence+Rules), m5_fm_rules (FM+Rules), m2_snapshot_llm (CurrentSnapshot+LLM), m6_fm_llm (FM+LLM), and m7_fm_llm_gated (FM+LLM+RulesGate). The gated LLM variant is included to separate FM-conditioned target selection from unconstrained LLM intervention severity.

\input{sections/Table3_MethodComparison.tex}

Figure~\ref{fig:task2_method_comparison} visualises the key metrics (precision, stability, and cost-adjusted score).
\begin{figure}[!t]
    \centering
    \includegraphics[width=0.9\linewidth]{figures/fig4_method_comparison}
    \caption{Task~II method comparison on key decision metrics.}
    \label{fig:task2_method_comparison}
\end{figure}

The previous Task~II numerical discussion has been removed because the LLM checkpoint files and raw assessments disagreed, and because the ungated FM+LLM policy was not comparable to the conservative rule engine. We will restore this paragraph after the clean LLM and rule-based reruns pass the audit script.

\input{sections/Table_b5_decision_CrisisPeriod.tex}

\paragraph{Early-warning timeline analysis.}
To determine whether FM alerts constitute genuine \emph{early warnings} or merely post-hoc damage reports, we conduct a per-week alert timeline analysis around the Terra/Luna (2022-05-09) and FTX (2022-11-07) events. Table~\ref{tab:alert_timeline} summarises the results.

\input{sections/Table_AlertTimeline.tex}

The early-warning timeline and b4_stress stress-test discussion are also pending clean regeneration. We keep the table hooks in place so the new artifacts can be inserted directly after rerun.

\input{sections/Table_b4_stress_Detail.tex}

\subsection{Ablation Study}

We validate the necessity of key design components through ablation experiments under both standard and stress conditions (Table~\ref{tab:ablation}).

\input{sections/Table_Ablation.tex}

\paragraph{Standard conditions (Panel~A).} Under clean 2025 test data, the confidence gate (A2) proves essential: removing it increases the false intervention rate from $0.000$ to $0.400$. The scenario engine (A3) is structurally necessary. Data-health gating (A1) and multi-horizon diversity (A6) show no measurable impact because data quality is high and $h=4$ dominates alert generation.

\paragraph{Stress conditions (Panels~B--C).} To test whether A1 and A6 are genuinely unnecessary or merely dormant, we evaluate them under adversarial conditions. For A1, we apply severe data degradation (80--95\% of features and edges removed, reducing $\mathrm{DH}_t$ to $0.59$--$0.75$) and isolate the data-health gate from the confidence gate by setting $\tau_{\mathrm{conf}}=0$. Under these conditions, disabling the data-health gate increases the false intervention rate from $0\%$ to $41$--$48\%$ (Panel~B), confirming that A1 functions as a \emph{safety mechanism that activates under data degradation}. For A6, we compare single-horizon ($h=4$) against multi-horizon ($h\in\{1,4,8,12\}$) monitoring during the Terra/Luna crisis period: multi-horizon generates $5\times$ more alerts while maintaining identical precision (Panel~C), confirming that A6 provides broader risk coverage when network dynamics are rapid. These results demonstrate that all four components are necessary for reliable operation: A2 and A3 are active in all conditions, while A1 and A6 are safety reserves that engage under data degradation and crisis dynamics, respectively.
