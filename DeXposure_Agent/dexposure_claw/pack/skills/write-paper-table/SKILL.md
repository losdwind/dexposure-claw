---
description: Generate paper-ready benchmark tables from DeXposure Claw outputs.
---

# Write Paper Table

Use this when converting benchmark JSON into Markdown, CSV, or LaTeX tables.

Rules:

- Preserve benchmark IDs and add human-readable labels.
- Do not average incomparable methods.
- Mark predictor-only methods as not applicable for decision-ticket metrics.
- Include run ID and split in captions or notes.
